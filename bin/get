#!/bin/bash

prj=$(basename $PWD)
cmd=../rrun-$prj
dir=/mnt/builds/$prj
if [ -x $cmd ]
then
    echo "now running $cmd"
    $cmd
else
    if [ -d $dir ]
    then
        echo "now running rsync $dir"
        rsync -a -v $dir/ .
    fi
fi
