#!/bin/bash

prj=$(basename $PWD)
cmd=../docs-$prj
upgrade=$SMITHHOME/$prj/upgrade*.bash
if [ -x $cmd ]
then
    echo "now running $cmd"
    $cmd
else
    if [ -f $upgrade ]
    then
        umask 022
        bash $upgrade
    else
        echo "upgrade script $upgrade does not exist"
    fi
fi
